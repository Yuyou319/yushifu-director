# 多模态节点画布产品 · 方案与架构文档（v3.1 便携版）

> 状态：**草案，待你确认后再进入开发**。本文档不含任何实现代码，仅确定方案、架构与路线。
> v3.1 变更：分发方式改为**便携 exe + 带密码 zip（解压即用，免安装）**。

---

## 0. 文档状态
- 阶段：需求对齐 → 方案确认（当前）
- 下一步：你确认本方案后，我再产出代码与构建脚本。

---

## 1. 产品目标与范围

| 项 | 内容 |
|---|---|
| 产品形态 | **Windows 桌面应用（便携版：解压即用）**，密码 zip 分发，内置节点式 AI 工作流画布 |
| 核心能力 | 多模态：文本 / 生图 / 生视频 / 生音频，模型全部走 **用户自己的 API** |
| 关键决策 | **不封装平台 Key、无后端、无服务器**；用户在自己设置里填 API 端点 + Key |
| 商业模式 | 卖「带密码的 zip 压缩包」（一次性工具）；密码即售卖钥匙 |
| 地域 | 国内用户为主 |
| 合规 | 最轻：无公网服务，免 ICP 备案 |

**用户视角**：收到 zip + 密码 → 解压 → 双击 exe 即用。在设置里填入自己的 API Key（文本/图/视频/音频各填各的），然后在画布上拖节点编排、生成。你的产品只是「画布工具」，额度与费用全由用户自己的 API 账号承担。

---

## 2. 技术路线

**Tauri + React + @xyflow/react（react-flow）**，纯前端桌面应用，无任何后端。

### 为什么这样
- 安装包小（~10MB），**可做成自包含 exe，解压即用**。
- 画布形态与 Dify 一致，界面 100% 可控。
- 用户 Key 只存在**本机**，调用直接发自本机到用户自己的 API，**不经过你的任何服务器**，无泄露风险、无运维。

### 技术栈
| 层 | 选型 |
|---|---|
| 桌面壳 | Tauri（Rust + 系统 WebView） |
| 前端 | React + TypeScript + Vite + **@xyflow/react** |
| 本地存储 | 用户 Key 存系统密钥库 / 加密本地文件 |
| 网络 | 桌面端直接 `fetch` 用户配置的 API 地址 |
| 构建 | `tauri build` 产出自包含 exe（前端资源已打入二进制）；再打包为**带密码 zip** 分发 |

---

## 3. 架构（无后端，便携）

```
用户 Windows 机器
 └─ 解压后的文件夹
       └─ app.exe（自包含，双击运行）
             ├─ 设置：填入【用户自己的】API 端点 + Key（本地加密保存）
             ├─ 画布：拖节点（开始 → LLM → 生图 → 生视频 → 生音频 → 结束）
             └─ 运行：节点直接 fetch 用户配置的 API（Key 仅留本机）
```
- **没有你的服务器**，没有域名、没有备案、没有 Key 泄露风险。
- 每个节点类型对应一类「用户 API 配置」：文本 / 生图 / 生视频 / 生音频。
- 运行前提：目标机需有 **WebView2 运行时**（Win10/11 基本自带；缺失时系统会提示安装，或构建时可选择打包运行时）。

---

## 4. API 配置设计（BYOK）

- 设置页分四个区：文本 LLM、生图、生视频、生音频。
- 每项含：API Base URL、API Key、模型名（可选）、其他参数。
- 节点运行时读取对应配置，拼请求体，`fetch` 用户 API。
- 支持「OpenAI 兼容」格式作为默认模板，用户改 URL/Key 即可，降低配置门槛。

---

## 5. 安全与隐私

- **用户 Key 仅存本机**（系统密钥库优先），不上传任何服务器。
- 调用直接本机 → 用户 API 服务商，产品方看不到、也拿不到用户 Key。
- 画布/工作流文件存本机（可导出/导入 `.json`）。

---

## 6. 售卖与分发（便携 zip + 密码）

- **卖什么**：一个**带密码的 zip 压缩包**（里面是便携 exe + 必要文件）。
- **分发**：把 zip + 密码发给买家；买家解压 → 双击 exe 即用，**无需安装**。
- **密码 = 你的"售卖钥匙"**：用户付钱你给密码。
  - ⚠️ 边界：zip 密码只是**访问门槛**，exe 解压后仍可被拷贝传播（弱保护，你已接受）。若要强防拷仍需后端，已放弃。
- **防拷贝（可选，仍无后端）**：
  - 本地许可码：启动校验一个 license key（本地规则校验，非联网），仅防小白随便拷。
  - 或干脆不校验，靠口碑/服务卖（很多独立工具如此）。
- **代码签名（可选）**：未签名 exe 首次运行可能被 SmartScreen 报「未知发布者」，点"仍要运行"即可；购 Windows 代码签名证书可消除警告（¥几百/年）。
- **备选形态**：也可做成纯静态网页包（连 Tauri 都不要，解压后用浏览器打开 index.html）——更简单，但 `file://` 下 `fetch`/模块有些限制，体验不如桌面 exe。推荐 Tauri 便携 exe。

---

## 7. 用户使用流程

1. 收到 zip + 密码 → 解压。
2. 双击 `app.exe` → 启动（首次可能被 SmartScreen 拦，点"仍要运行"）。
3. 设置里填入自己的 API 端点 + Key（四个模态各自填）。
4. 新建工作流 → 拖节点编排。
5. 点「运行」→ 本机直接调用户自己的 API，结果回显画布。
6. 保存 / 导出工作流（`.json`）。

---

## 8. 合规（最轻）

| 合规项 | 是否必须 | 说明 |
|---|---|---|
| ICP 备案 | ❌ | 无公网服务，不需 |
| 服务器实名 | ❌ | 无服务器 |
| 内容安全审核 | 可选 | 调用发生在用户与自己的 API 之间，平台方不接触内容 |
| 生成式 AI 备案 | 视情况 | 作为「工具」提供，通常不视为对外提供 AI 服务；上线前可再确认 |

---

## 9. 分阶段路线图

- **M1 · MVP**：Tauri 壳 + react-flow 画布 + 文本(LLM)节点 + 本地 API 配置 → 本机跑通、便携 exe 可运行。
- **M2 · 多模态**：叠加生图 / 生视频 / 生音频节点 + 配置导入导出。
- **M3 · 产品化**：便携 zip 打包 + 密码分发流程、可选本地许可码、使用文档、更新提示。

---

## 10. 成本估算
| 项目 | 预估 |
|---|---|
| 代码签名证书（可选） | ¥几百/年（不买也能跑，仅警告） |
| 分发/官网（可选） | 按量，可省 |
| 服务器/域名/备案 | **¥0**（无后端） |
| 合计 | 近零（可选签名证书） |

---

## 11. 风险与待确认
1. **WebView2 依赖**：目标机若无 WebView2 需安装；构建时可选择打包运行时（增大体积）。
2. **配置门槛**：用户需自己申请各 API Key，可能劝退小白 → 文档/模板给常见服务商填法。
3. **防拷贝**：选纯本地许可码（弱）/ 不校验（靠卖服务）。
4. **代码签名**：是否购买（影响安装信任度）。
5. **节点生态**：先支持哪些模态/哪些 API 格式（建议先 OpenAI 兼容 + 国内主流）。

---

## 12. 下一步
- 你确认本方案 → 我产出（仍按"先确认后开发"）：
  1. Tauri + React + react-flow 桌面端脚手架（画布 + 节点 + 运行）；
  2. 本地 API 配置（四个模态）+ 密钥本机存储；
  3. 工作流导入/导出（`.json`）；
  4. 便携 exe 构建 + 打包为密码 zip 的说明。
- 在此之前**不写实现代码**，仅对齐方案。
