# HBv1.0 全站功能测试报告

测试方式：Chromium 无头浏览器 + 页面内 JS 驱动真实点击 / 输入 / 上传，全程监听 `error`、`unhandledrejection`、`console.error`、`console.warn`。
测试地址：`http://localhost:4173`（生产构建 `npm run build` 产物）

---

## 一、发现并修复的严重问题

| # | 问题 | 严重级别 | 原因 | 修复 |
|---|---|---|---|---|
| 1 | 从「生图」切到「生视频」时整页崩溃白屏，之后所有页面全挂 | **致命** | `GeneratePage` 切换功能组时 `modeId` 状态残留上一个组的子模式，`group.modes.find()` 返回 `undefined`，读取 `mode.modality` 抛错 | ① 给 `GeneratePage` 加 `key={id}` 强制重建；② `mode` 增加 `?? group.modes[0]` 兜底；③ 新增 `useEffect` 在 group 变化时重置 `modeId` |
| 2 | 侧边导航缺少「剧本 / 分镜」「角色一致性」两个功能组 | 中 | `TOOLS` 数组漏配，只能从首页功能地图进入 | 已补进「创作工具」分组，并补 `character` 图标映射 |

---

## 二、页面渲染测试（21 个导航入口）

全部渲染成功，无白屏 / 黑屏，内容长度正常：

| 分类 | 页面 | 结果 |
|---|---|---|
| 导演流程 9 步 | 创意 / 策划 / 剧本 / 资产 / 分镜 / Prompt / 生成 / 剪辑 / 成片 | ✅ 全部正常 |
| 创作工具 | 工作台、生图、生视频、生音频、剧本·分镜、角色一致性、图像工具、视频工具 | ✅ 全部正常 |
| 其它 | 无限画布、资产库、项目、设置 | ✅ 全部正常 |

---

## 三、子模式测试（7 组共 44 个子模式）

每个子模式都渲染出**不同长度、不同结构**的参数界面，证明各自 UI 独立：

- **生图（8）**：文生图、图生图、局部重绘、画面扩展、多角度生成、智能打光、角色三视图、宫格分镜 ✅
- **生视频（7）**：文生视频、图生视频、**多参生视频**、**首尾帧生视频**、视频续写延长、对口型、视频转视频 ✅
- **生音频（4）**：文本配音、声音克隆、音乐生成、音效生成 ✅
- **剧本 / 分镜（4）**：AI 剧本、分镜生成、故事板、导演台机位参数 ✅
- **角色一致性（3）**：角色三视图、风格锁定、一致性多图 ✅
- **图像工具（10）**：超分放大、画面扩展、多角度生成、智能打光、局部重绘、智能擦除、智能抠图、宫格切分、标注打标、裁剪 ✅
- **视频工具（8）**：视频高清放大、帧率提升、视频分镜解析、基础剪辑、视频转音频、视频配音、字幕烧录、合成导出 ✅

---

## 四、生成能力实测（演示模式，端到端跑通）

| 场景 | 输入 | 结果 |
|---|---|---|
| 文生图 | 提示词 | 任务完成，产出 2 张图，结果卡片渲染 ✅ |
| 图生图 | 提示词 + **上传参考图** | 任务完成，产出 2 张图 ✅ |
| 文生视频 | 提示词 | 任务完成，产出 1 个视频，播放器渲染 ✅ |
| **首尾帧生视频** | 提示词 + **上传首帧 + 尾帧** | 两帧预览正常，任务完成，产出 1 个视频 ✅ |
| **多参生视频** | 提示词 + **参考位上传素材** | 槽位缩略图正常，任务完成，产出 1 个视频 ✅ |
| 文本配音 | 提示词 | 任务完成，产出音频，播放器渲染 ✅ |
| AI 剧本 | 提示词 | 任务完成，产出文本 ✅ |

---

## 五、交互与组件测试

| 功能 | 结果 |
|---|---|
| 顶部搜索过滤导航 | ✅ 输入后导航实时过滤 |
| 项目切换下拉 | ✅ 正常 |
| 演示 / 真实 API 切换 | ✅ 正常 |
| 设置齿轮跳转 | ✅ 正常 |
| chips / 比例 / 时长 / 风格选择 | ✅ 点击 8+ 个，状态切换正常 |
| 开关（switch） | ✅ 切换正常 |
| 滑块（slider） | ✅ 赋值 0.8 生效 |
| 上传组件拖拽位 | ✅ 上传 → 转 data URL → 缩略图预览 |
| 资产库：标签页（全部/图片/视频/音频/文本） | ✅ 分类计数正确 |
| 资产库：收藏 | ✅ 切换正常 |
| 资产库：删除 | ✅ 5 → 4 |
| 资产库：灯箱大图 | ✅ 打开 / 关闭正常 |
| 项目：创建 | ✅ 创建成功并自动进入创意阶段（设计如此） |
| 项目：重命名 / 打开 | ✅ 正常 |
| 设置页：输入框与开关 | ✅ 22 个输入、2 个开关，赋值生效 |
| 导演 9 阶段按钮 | ✅ 每阶段点击 6 个操作按钮，0 报错 |
| 首页快捷入口（10 个） | ✅ 全部跳转成功、内容非空 |
| 首页功能地图（7 个） | ✅ 全部跳转成功 |

---

## 六、画布（无限画布）测试

| 项目 | 结果 |
|---|---|
| 节点库渲染 | ✅ 25 个节点按钮，**每个都带独立图标**（25 个 svg） |
| 模板库图标 | ✅ 6 个模板各用不同图标 |
| 添加节点 | ✅ 点击节点库 → 节点出现在画布（0 → 2） |
| 加载模板 | ✅ 一键铺出 4 个节点 + 1 条连线 |
| React Flow 控件 / MiniMap | ✅ 渲染正常 |

---

## 七、稳定性

| 检查项 | 结果 |
|---|---|
| `error` 事件 | **0** |
| `unhandledrejection` | **0** |
| `console.error` | **0** |
| `console.warn`（含 React key 警告） | **0** |
| 刷新后数据持久化（localStorage） | ✅ 资产 4 条、项目 2 个均保留 |
| TypeScript 编译 | ✅ `tsc --noEmit` 通过 |
| 生产构建 | ✅ `vite build` 通过 |

---

## 八、结论

**可以交付。** 全站 21 个页面、44 个子模式、9 个导演阶段、7 类生成能力、画布、资产库、项目、设置全部实测通过，无白屏 / 黑屏 / 控制台报错。

测试中发现的两个真实缺陷（功能组切换白屏崩溃、导航缺少两个功能组）已修复并回归验证。

### 已知非缺陷行为

- 画布「保存 / 载入 / 导出」使用浏览器原生 `alert` 提示，属原有设计，未改动
- 项目删除、资产清空使用 `confirm` 二次确认，属原有设计
- 未配置 API Key 时走本地演示生成（演示模式），属设计预期

### 版本

- `v1.0.0` — `61e0a1c`
- `v1.0.1` — `595876f`（含本次修复）· 已打 tag
