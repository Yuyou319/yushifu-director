# HBv1.0 全站功能测试报告（最终版）

- 测试方式：Chromium 无头浏览器驱动真实点击 / 输入 / 上传 / 拖拽，全程监听 `error`、`unhandledrejection`、`console.error`、`console.warn`
- 测试地址：`http://localhost:4173`（`npm run build` 生产构建产物）
- 测试版本：**v1.0.3**（`e8a5c1a`）

---

## 一、测试中发现并修复的问题

| # | 问题 | 级别 | 原因 | 修复 |
|---|---|---|---|---|
| 1 | 从「生图」切到「生视频」整页白屏崩溃，之后所有页面全挂 | **致命** | `GeneratePage` 切换功能组时 `modeId` 残留上一组子模式，`find()` 返回 `undefined` 后读 `mode.modality` 抛错 | ① `key={id}` 强制重建 ② `mode` 加 `?? group.modes[0]` 兜底 ③ `useEffect` 在 group 变化时重置 `modeId` |
| 2 | 侧边导航缺少「剧本 / 分镜」「角色一致性」 | 中 | `TOOLS` 漏配，只能从首页功能地图进入 | 补进「创作工具」分组并补图标映射 |
| 3 | 画布节点拖动 / 点击后偶发整页黑屏 | **高** | 无错误边界，任一渲染异常就会让 React 卸载整棵树 | 新增 `ErrorBoundary`：全局包一层 + 画布单独包一层（带「重新加载 / 重置画布」恢复 UI），异常不再黑屏 |
| 4 | 本机保存的画布脏数据（position 为 null/字符串、data 为 null、连线指向不存在的节点）会让 React Flow 崩溃 | 高 | 读取 localStorage 后未校验直接渲染 | 新增 `sanitizeNodes` / `sanitizeEdges`，加载时过滤坏节点与孤儿连线 |
| 5 | 点击「复制」产生未处理的 Promise 拒绝（`NotAllowedError: writeText`） | 中 | `navigator.clipboard.writeText` 未 `catch`，无剪贴板权限时直接抛到全局 | 新增 `lib/clipboard.ts` 的 `copyText()`：Clipboard API + `execCommand` 降级，全程吞异常 |
| 6 | 画布「运行」失败时异常无人接管 | 中 | `run()` 直接 `await runGraph()`，`running` 可能卡死 | `try/catch/finally` 包裹，失败在工具栏显示「运行失败：xxx」 |

---

## 二、页面渲染（21 个导航入口）

```
创意=433  策划=454  剧本=198  资产=118  分镜=107
Prompt=345 生成=673 剪辑=501 成片=393
工作台=685 生图=627 生视频=631 生音频=437
剧本/分镜=430 角色一致性=528 图像工具=482 视频工具=455
无限画布=341 资产库=45 项目=63 设置=644
```
✅ 21 / 21 全部渲染成功，`allRendered=true`，无白屏 / 黑屏，`errs=0 logs=0`
（资产库 / 项目页数字小是因为当时无数据，属空状态，非空页面）

---

## 三、子模式（7 组 44 个）

每组子模式均渲染出**不同长度、不同结构**的参数界面，证明 UI 各自独立：

- **生图 8**：文生图 330 / 图生图 276 / 局部重绘 231 / 画面扩展 191 / 多角度生成 331 / 智能打光 295 / 角色三视图 267 / 宫格分镜 317
- **生视频 7**：文生视频 298 / 图生视频 221 / **多参生视频 451** / **首尾帧生视频 336** / 视频续写延长 174 / 对口型 209 / 视频转视频 220
- **生音频 4**：文本配音 200 / 声音克隆 191 / 音乐生成 192 / 音效生成 138
- **剧本 / 分镜 4**：AI 剧本 174 / 分镜生成 256 / 故事板 355 / 导演台机位参数 368
- **角色一致性 3**：角色三视图 307 / 风格锁定 274 / 一致性多图 272
- **图像工具 10**：超分放大 146 / 画面扩展 169 / 多角度生成 172 / 智能打光 226 / 局部重绘 182 / 智能擦除 170 / 智能抠图 138 / 宫格切分 124 / 标注打标 125 / 裁剪 127
- **视频工具 8**：视频高清放大 121 / 帧率提升 144 / 视频分镜解析 137 / 基础剪辑 145 / 视频转音频 129 / 视频配音 212 / 字幕烧录 180 / 合成导出 190

✅ 44 / 44 正常，`errs=0 logs=0`

---

## 四、生成能力（端到端跑通）

| 场景 | 输入 | 结果 |
|---|---|---|
| 文生图 | 提示词 | 2 张图，任务完成 ✅ |
| 图生图 | 提示词 + **上传参考图** | 预览 1 张，产出 2 张图 ✅ |
| 文生视频 | 提示词 | 1 个视频 + 播放器 ✅ |
| **首尾帧生视频** | 提示词 + **首帧 + 尾帧上传** | 2 个预览，产出 1 个视频 ✅ |
| **多参生视频** | 提示词 + **参考位上传** | 槽位缩略图 1 张，产出 1 个视频 ✅ |
| 文本配音 | 提示词 | 音频 + 播放器 ✅ |
| AI 剧本 | 提示词 | 文本结果 ✅ |

---

## 五、画布（无限画布）

| 项目 | 结果 |
|---|---|
| 节点库 | 25 个按钮，**每个都有独立图标**（25 个 svg） ✅ |
| 模板库 | 6 个模板各自不同图标 ✅ |
| 添加节点 | 0 → 3 ✅ |
| 连续拖拽压测 | 3 个节点 × 4 轮拖拽 + 每轮点击选中，无异常 ✅ |
| 加载模板 | +2 节点 / 1 条连线 ✅ |
| 点击运行 | 完成，无异常 ✅ |
| 错误兜底 | `errFallback=0`（正常路径不触发），顶栏与节点库始终可见 ✅ |
| **脏数据防护** | 注入 position 为 null、data 为 null 的坏节点 + 孤儿连线后重载：坏数据被过滤（3 → 1 有效节点，2 → 0 有效连线），页面照常渲染，无黑屏 ✅ |

---

## 六、其它功能

| 功能 | 结果 |
|---|---|
| 首页快捷入口 | 17 / 17 跳转成功且内容非空 ✅ |
| 首页功能地图 | 7 / 7 跳转成功且内容非空 ✅ |
| 资产库标签页 | 全部 9 / 图片 4 / 视频 3 / 音频 1 / 文本 1，计数正确 ✅ |
| 资产收藏 | ✅ |
| 资产删除 | 9 → 8 ✅ |
| 资产灯箱 | 打开 / 关闭 ✅ |
| 项目创建 | 0 → 1，自动进入创意阶段（设计如此）✅ |
| 设置页 | 22 个输入框、2 个开关，赋值与切换生效 ✅ |
| 导演 9 阶段 | 每阶段点击 8 个操作按钮，共 179 次点击，**0 报错** ✅ |
| 复制按钮 | 修复后 `errs=0` ✅ |

---

## 七、稳定性

| 检查项 | 结果 |
|---|---|
| `error` 事件 | **0** |
| `unhandledrejection` | **0** |
| `console.error` | **0** |
| `console.warn`（含 React key 警告） | **0** |
| 刷新后持久化 | 资产 2 条、项目 1 个均保留 ✅ |
| TypeScript 编译 | `tsc --noEmit` 通过 ✅ |
| 生产构建 | `vite build` 通过 ✅ |

---

## 八、结论

**通过，可以交付。**

- 21 个页面、44 个子模式、9 个导演阶段、7 类生成能力、画布、资产库、项目、设置全部实测通过
- 无白屏 / 黑屏 / 控制台报错
- 本轮共修复 6 个问题（含 1 个致命、1 个高危），全部已回归验证

### 已知非缺陷行为（原有设计，未改动）

- 画布「保存 / 载入 / 导出」使用浏览器原生 `alert` 提示
- 项目删除、资产清空使用 `confirm` 二次确认
- 画布节点需手动点「保存」才会写入本机（无自动保存）
- 未配置 API Key 时走本地演示生成

### 版本

| 版本 | Commit | 说明 |
|---|---|---|
| **v1.0.3** | `e8a5c1a` | 画布错误边界 + 脏数据清洗 + 安全剪贴板复制 |
| v1.0.2 | `e194486` | 测试报告 |
| v1.0.1 | `595876f` | 修复功能组切换白屏崩溃、补齐导航、节点库图标 |
| v1.0.0 | `61e0a1c` | 深色霓虹玻璃主题 + lucide 矢量图标 |

全部版本均已打 tag，可用 `git reset --hard <tag>` 回滚。
