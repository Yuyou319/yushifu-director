# 多模态节点画布 · 产品设计说明书（功能 / 流程图 / 架构）

> 说明：本文档用于让你看清产品长什么样、怎么用、怎么搭。暂不写实现代码，先对齐设计与功能范围。
> 节点库已参考主流平台（LibTV、ComiComi、ComfyUI 视频工作流）补齐「分镜 / 故事板 / 导演台 / 角色一致性」等能力。
> 产品定位：**对标 LibTV 的本地版（LibTV 本地版）**——桌面、BYOK、节点式多模态创作工作台。

---

## 1. 产品一句话说明（对标 LibTV 本地版）
**一个 Windows 桌面工具**：打开就是一块「节点画布」，你把自己的 API（文本 / 生图 / 生视频 / 生音频）填进设置，然后在画布上拖节点、连线，编排成多模态生成流程，点运行即可得到图文音视频结果。可理解为「LibTV 的本地 / 离线版」——同样的无限画布节点式创作，但模型走你自己的 API，无后端、Key 只存本机。

---

## 2. 产品定位与目标用户
- **定位**：离线优先的「多模态 AI 工作流画布」，用户自带 API（BY,OK）。
- **目标用户**：有各家 AI API 账号、想可视化编排的人；做短视频/配音/批量生成内容的工作室；想做「一个人就是个剧组」的创作者。

---

## 3. 核心功能清单（齐全）

| 模块 | 功能 |
|---|---|
| **画布** | 无限画布、平移/缩放、网格、框选、对齐辅助线 |
| **节点操作** | 拖入、删除、复制/粘贴、撤销/重做、分组、备注 |
| **节点库（完整）** | 见第 4 节（基础 / 剧本策划 / 多模态生成 / 图像编辑 / 视频编辑 / 角色一致 / 合成导出） |
| **节点配置** | 右侧配置面板；每个节点可填参数、提示词、选择已存 API 配置；「测试连接」 |
| **连线数据流** | 上游节点输出自动作为下游输入；类型校验（文本/图片/视频/音频） |
| **运行引擎** | 按拓扑顺序/并行执行；运行中高亮、进度、取消；错误捕获与提示 |
| **结果预览** | 文本展示、图片预览、视频播放、音频播放；可下载/复制 |
| **工作流管理** | 新建、保存（本地）、打开、导出 `.json`、导入、模板库 |
| **API 配置** | 各模态：Base URL、Key、默认模型、参数；内置常见服务商预设；加密存本机 |
| **日志/历史** | 运行日志、耗时、调用次数统计；历史图片/视频/音频可调用 |
| **设置** | 主题（亮/暗）、默认超时、并发数、导出路径 |
| **导入导出** | 工作流 `.json`、节点模板分享 |

---

## 4. 节点库（完整版，参考主流平台补齐）

### 4.1 基础 / 流程控制
| 节点 | 输入 | 输出 | 说明 |
|---|---|---|---|
| 开始 | 用户输入/变量 | 文本/参数 | 流程入口，定义初始变量 |
| 结束 | 任意 | 结果 | 流程出口，汇总结果 |
| 变量 / 常量 | — | 值 | 复用参数 |
| 条件分支 | 任意 | 分支 | 按规则走不同路径 |
| 循环 / 批量 | 集合 | 逐项 | 批量生成（如多分镜） |
| 并行分支 | 上游 | 多路 | 多镜头并行渲染（提速） |
| 合并 | 多路 | 聚合 | 汇聚多路结果 |
| HTTP 通用 | 任意 | 任意 | 调任意 API（高级） |
| 延时 / 等待 | — | — | 等待异步任务回调 |

### 4.2 剧本与策划（分镜 / 故事板 / 导演台）
| 节点 | 输入 | 输出 | 说明（对应 LibTV「导演台/故事板」） |
|---|---|---|---|
| 剧本生成（脚本） | 梗概/灵感 | 结构化剧本 | 调 LLM 生成分幕、台词、节奏 |
| 分镜生成 | 剧本/段落 | 分镜列表（景别/镜头/台词） | 剧本→分镜脚本（LibTV 核心） |
| 故事板 | 分镜列表 | 分镜图（批量） | 逐条调生图，输出镜头画面 |
| 角色设定 / 三视图 | 角色描述 | 角色三视图图 | 生成正面/侧面/背面（LibTV Slash） |
| 镜头/导演台参数 | — | 参数包 | 机位/视角/打光预设（喂给生图/生视频） |
| 剧情推演 / 画面推演 | 文本 | 拓展文本 | LLM 辅助叙事/镜头预演 |
| 多机位九宫格 | 场景描述 | 9 宫格图 | 一次生成多机位分镜（LibTV Slash） |
| 25 宫格连贯分镜 | 场景描述 | 25 宫格图 | 连贯分镜批量预览 |

### 4.3 多模态生成
| 节点 | 输入 | 输出 | 说明 |
|---|---|---|---|
| 文本（LLM） | 文本/变量 | 文本 | 调用户文本 API（默认 OpenAI 兼容） |
| 生图 | 文本提示 | 图片 | 调用户图像 API |
| 文生视频 | 文本提示 | 视频 | 调用户视频 API |
| 图生视频 | 图片/文本 | 视频 | 图+提示生成动态视频（LibTV/ComiComi 图生视频） |
| 生音频 / 配音（TTS） | 文本 | 音频 | 调用户配音/语音合成 API |

### 4.4 图像编辑（来自 LibTV 图像工具集 10 项）
| 节点 | 输入 | 输出 | 说明 |
|---|---|---|---|
| 放大（超分） | 图片 | 图片 | 2/4/6 倍放大 |
| 扩图（Outpaint） | 图片/方向 | 图片 | 扩展画面边界 |
| 多角度生成 | 图片/视角 | 图片 | 15 环绕点位视角（保持同一主体） |
| 打光 | 图片/光位 | 图片 | 26 主光点位 + 智能打光 |
| 重绘（Inpaint） | 图片/蒙版 | 图片 | 局部重绘 |
| 擦除 | 图片/区域 | 图片 | 去水印/去物 |
| 抠图 | 图片 | 透明图 | 主体抠出 |
| 宫格切分 | 图片 | 多图 | 把宫格图拆成单镜头 |
| 标注（打标） | 图片 | 标签 | 生成画面标签/描述 |
| 裁剪 | 图片 | 图片 | 裁取区域 |

### 4.5 视频编辑（来自 LibTV 视频工具集）
| 节点 | 输入 | 输出 | 说明 |
|---|---|---|---|
| 视频高清放大 | 视频 | 视频 | 2/4/6 倍 + 帧率提升 30/60/90fps |
| 视频分镜解析 | 视频 | 分镜片段 | 解析成多个镜头（便于再编辑） |
| 基础剪辑（裁取） | 视频/区间 | 视频 | 片段裁取/拼接 |
| 视频转音频 | 视频 | 音频 | 提取/分离音轨 |
| 视频配音 | 视频/音频 | 视频 | 画面+配音合成 |

### 4.6 角色一致性
| 节点 | 输入 | 输出 | 说明 |
|---|---|---|---|
| 角色参考锁定 | 参考图 | 一致性参数 | 主体库锁定（喂给后续生图/生视频） |
| 角色三视图 | 描述/参考 | 三视图 | 见 4.2 |
| 风格锁定 | 风格图 | 参数 | 统一画风 |

### 4.7 合成 / 导出
| 节点 | 输入 | 输出 | 说明 |
|---|---|---|---|
| 视频合成 | 画面+音频 | 成片 | 多段视频/配音合成 |
| 字幕烧录 | 视频/文本 | 视频 | 叠加字幕 |
| 导出 | 任意 | 文件 | 导出本地（视频/图/音频/工程） |

---

### 4.8 模型切换 UX（对齐 LibTV：节点级切换，不退出项目）

- **在节点上切换**：每个生成节点（生图 / 生视频 / 生音频 / LLM）**双击打开配置面板**，面板内有「模型」下拉，可为每个节点单独指定不同模型——同一条流里图片用 A、视频用 B，符合 LibTV「去中心化节点级」做法。
- **默认模型**：在「设置 → 各模态 API 配置」里设该模态默认模型；新建节点自动套用，节点面板仍可覆盖。
- **模型来源（BYOK）**：可用模型来自你配置的 API 服务商；内置常见服务商预设（OpenAI 兼容、各家国内模型），列出其模型名；也支持手动填模型名。
- **对齐 LibTV 的要点**：模型选择完全发生在节点面板内，**没有全局 / 项目级切换入口**，便于混合多模型编排。

## 5. 主流平台节点功能对照（来源说明）

| 能力 | 来源平台 | 我们在产品中的落地 |
|---|---|---|
| 文本/图片/视频/音频/脚本 5 类基础节点 | LibTV 无限画布 | 见 4.1 / 4.3 |
| 分镜系统 / 故事板 / 批量转分镜图 | LibTV、ComiComi | 分镜生成 + 故事板节点（4.2） |
| 3D 导演台 / 镜头规划 / 机位打光 | LibTV 导演台 | 镜头/导演台参数节点（4.2） |
| 图像工具集（放大/扩图/多角度/打光/重绘/擦除/抠图/宫格/标注/裁剪） | LibTV | 4.4 全部 |
| 视频工具集（放大/帧率/分镜解析/剪辑） | LibTV | 4.5 全部 |
| 角色一致性 / 三视图 / 主体库 | LibTV、ComiComi | 4.6 |
| 图生视频 | LibTV、ComiComi | 文生视频 + 图生视频（4.3） |
| 并行分支 / 模板化批量生产 | LibTV 进阶 | 4.1 并行分支 + 模板库 |
| 从剧本到成片 5 节点流程（脚本解析→分镜→渲染→音频→导出） | LibTV 基础流 | 完整链路示例（见 6/7） |

> 说明：以上平台本身是「平台内嵌模型」；我们的产品把这些能力**抽象成节点，调用你自己配置的 API**，因此不受平台模型限制，且无限画布/节点编排逻辑一致。

---

## 6. 使用操作流程图（文字）

```
[ 双击 app.exe 启动 ]
        │
        ▼
[ 设置页：填入 文本/生图/生视频/生音频 的 API 端点 + Key ]  ── 本地保存
        │
        ▼
[ 新建工作流 ] ──▶ 画布出现「开始」节点
        │
        ▼
[ 从节点库拖入 ]：
   剧本生成 → 分镜生成 → 故事板(批量生图) → 图生视频 → 生音频 → 视频合成 → 结束
   （中间可插 图像编辑/视频编辑/角色锁定/条件/循环 节点）
        │
        ▼
[ 连线 ]：上游输出 → 下游输入（数据按线流动，类型校验）
        │
        ▼
[ 选中节点 → 右侧配置 ]：模型、参数、提示词、参考图；可「测试」校验连通
        │
        ▼
[ 点「运行」 ] ──▶ 运行引擎按拓扑顺序/并行执行
        │
        ├─▶ 剧本/分镜：调用户 LLM API → 文本
        ├─▶ 故事板/生图：调用户图像 API → 图片
        ├─▶ 图生视频：调用户视频 API → 视频
        ├─▶ 生音频：调用户配音 API → 音频
        └─▶ 视频合成：画面+音频 → 成片
        │
        ▼
[ 结果面板 ]：预览 / 下载 / 复制；可继续调整重跑
        │
        ▼
[ 保存工作流 或 导出 .json ] ／ [ 导入 / 套用模板 ]
```

---

## 7. 系统架构图（ASCII）

```
┌──────────────────────────────────────────────────────────────┐
│                     Windows 桌面应用 (Tauri)                   │
│                                                               │
│  ┌─────────────────────── React 前端 ─────────────────────┐   │
│  │  ┌────────────┐  ┌────────────┐  ┌──────────────┐     │   │
│  │  │ react-flow │  │  设置页    │  │  结果预览    │     │   │
│  │  │   画布     │  │ (API配置)  │  │(图/视/音/文) │     │   │
│  │  └────────────┘  └────────────┘  └──────────────�  │     │   │
│  │  ┌────────────┐  ┌────────────┐                    │     │   │
│  │  │ 节点库面板 │  │ 运行/日志  │                    │     │   │
│ ,  │  └────────────┘  └────────────┘                    │     │   │
│  └─────────────────────────────────────────────────────────┘ │
│                           │                                   │
│            ┌──────────────┼──────────────┐                    │
│            ▼              ▼              ▼                    │
│   ┌────────────┐  ┌────────────┐  ┌────────────────────┐     │
│   │ 本地配置   │  │ 工作流文件 │  │  运行引擎(本地)    │     │   │
│   │ 存储(Key)  │  │  .json     │  │  fetch 用户API     │     │   │
│   └────────────┘  └────────────┘  └────────────────────┘     │
└───────────────────────────────┬───────────────────────────────┘
                                │  HTTPS / fetch（携带用户 Key）
                                ▼
        ┌───────────────────────────────────────────────┐
        │         用户自己的 API（不经过任何服务器）       │
        │  文本 LLM │ 生图 │ 文/图生视频 │ 配音（各家）   │
        └───────────────────────────────────────────────┘
```

---

## 8. 运行时数据流（典型「剧本→成片」）

```
[开始] → [剧本生成] → [分镜生成] ─┬─▶ [故事板/生图] ─▶ [图生视频] ─┐
                                   │                                ├─▶ [视频合成] → [结束]
                                   └─▶ [角色锁定] ──────────────────┘
                                                    [生音频] ──▶ (合成音频轨)
```

- 每个节点的「API 配置」从设置页读取（用户填的 Key 仅本机）。
- 连线决定数据走向；类型不匹配时连线被禁止或运行时报错。
- 全程无后端，请求直接从本机发往用户自己的 API。

---

## 9. 技术栈与项目结构

| 层 | 选型 |
|---|---|
| 桌面壳 | Tauri（Rust + WebView） |
| 前端 | React + TypeScript + Vite + `@xyflow/react` |
| 状态 | Zustand / Redux（画布与配置） |
| 存储 | 配置存系统密钥库 / 加密 JSON；工作流存本地 `.json` |
| 网络 | 原生 `fetch` 调用用户 API |
| 构建 | `tauri build` → 自包含 exe（便携） |

```
src/
 ├─ main.tsx              入口
 ├─ App.tsx               布局（画布 + 侧栏 + 配置面板）
 ├─ canvas/               react-flow 画布、连线逻辑
 ├─ nodes/                各节点组件（按 4.1~4.7 分类）
 ├─ panels/               节点库、配置面板、结果预览
 ├─ settings/             API 配置页
 ├─ engine/               运行引擎（拓扑执行、fetch、并行）
 ├─ store/                全局状态
 └─ storage/              本地配置/工作流读写
src-tauri/                Tauri 壳（打包 exe）
```

---

## 10. 界面布局（示意）

```
┌──────┬────────────────────────────────────┬────────────┐
│ 节点 │                                    │  配置/结果  │
│ 库   │          画布（react-flow）         │  面板       │
│      │  [开始]→[剧本]→[分镜]→[故事板]     │             │
│ 剧本 │           →[图生视频]→[生音频]     │  节点参数   │
│ 分镜 │                    →[视频合成]→[结束]│  运行日志   │
│ 生图 │                                    │  结果预览   │
│ 生视频│                                    │             │
│ 生音频│                                    │             │
│ 图像 │                                    │             │
│ 视频 │                                    │             │
│ 角色 │                                    │             │
└──────┴────────────────────────────────────┴────────────┘
   顶部：新建/保存/运行/导入导出          底部：状态栏
```

---

## 11. 开发计划（先做齐全功能）
- **阶段一（MVP）**：Tauri 壳 + 画布 + 文本/生图/生视频/生音频 + API 配置 + 运行 + 预览。
- **阶段二（创作流）**：剧本/分镜/故事板/图生视频/角色锁定/条件/循环/并行。
- **阶段三（编辑增强）**：图像编辑 10 项 + 视频编辑 + 合成导出 + 模板库。
- **阶段四（完善）**：撤销重做、日志统计、主题、便携 exe 构建。

---

## 12. 待你确认
1. 节点库（4.1~4.7）是否覆盖你想要的？要不要再加「定时/脚本/字幕翻译」？
2. 要不要内置「模板库」（剧本→成片 等常用流程）？
3. 界面布局/交互有想调整的地方吗？

确认后我再进入开发，按阶段一先交付可运行版本。
