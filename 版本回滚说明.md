# HBv1.0 版本回滚说明

## 当前版本

| 项目 | 值 |
|---|---|
| 版本 | v1.0.0 |
| Commit | `61e0a1c` |
| 分支 | `main` |
| 内容 | 深色霓虹玻璃主题 + lucide 矢量图标 + 功能地图/画布/项目/资产全站风格统一 |

## 核心回滚命令（在项目根目录执行）

```bash
# 1. 查看所有已保存版本
git log --oneline

# 2. 查看当前改动（有没有东西没提交）
git status

# 3. 彻底回滚到 v1.0.0（丢弃之后所有改动，谨慎）
git reset --hard v1.0.0

# 4. 只回滚某个文件 / 目录到 v1.0.0
git checkout v1.0.0 -- src/
git checkout v1.0.0 -- src/index.theme.css

# 5. 临时看看旧版本（看完可切回来，不影响当前代码）
git checkout v1.0.0
git checkout main
```

## 保存新版本

以后每完成一个阶段，在根目录执行：

```bash
git add -A
git -c user.name="CodeBuddy" -c user.email="codebuddy@local" commit -m "v1.0.1 说明本次改动"
git -c user.name="CodeBuddy" -c user.email="codebuddy@local" tag -a v1.0.1 -m "v1.0.1 说明"
```

## 物理备份

- 目录：`backups/`
- 文件：`HBv1.0-v1.0.0-src.zip`（含 `src/`、`scripts/`、`concept/`、配置和文档）
- 用法：解压后覆盖项目根目录对应文件即可还原

## 说明

- `node_modules/`、`dist/`、`src-tauri/target/`、`backups/` 和所有 `*.log` 已加入 `.gitignore`，不进版本库
- 只回滚界面/代码，不影响本机数据（资产、任务、设置保存在浏览器 localStorage）
